{
  "name": "slshx-starter",
  "version": "1.0.0",
  "type": "module",
  "module": "dist/index.mjs",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env=development",
    "dev:local": "wrangler  --local dev --env=development # this is broken atm lmfao",
    "deploy:global": "node --no-warnings --experimental-vm-modules scripts/deploy.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.0",
    "slshx": "^0.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20221111.1",
    "esbuild": "^0.17.0",
    "miniflare": "^2.11.0",
    "wrangler": "^2.7.1"
  }
}
